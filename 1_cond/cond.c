#include <stdio.h>

int main() {

	int a = 5;
	int x, y;
	printf("type x:\n");
	scanf("%d, %d", &x, &y);
	printf("x = %d\n", x);
	// && - AND
	// || - OR
	// != - not equal
	// == - equal
	// >=, <=

/*
	if ()
		printf("1");
	else
		printf("0");
/*
	if (a == 5 && b != 2) {
		printf("yes\n");
		printf("\n");
	} else if (a > 5)
		printf("No\n");
	else
	*/

	// todo switch дописать пример

	return 0;
}
